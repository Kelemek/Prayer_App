/** Shared layout for settings section child components (multi-root templates + parent gap). */
export const USER_SETTINGS_SECTION_HOST_STYLES = [
  ':host { display: flex; flex-direction: column; gap: 1rem; }',
] as const;
